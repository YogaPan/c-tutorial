#include <stdio.h>
#include <string.h>
#include <zlib.h>

int main(void)
{
	/* gzFile *fi = (gzFile *)gzopen("file.gz","wb"); */
	/* gzwrite(fi, "my decompressed data", strlen("my decompressed data")); */
	/* gzclose(fi); */
	return 0;
}
